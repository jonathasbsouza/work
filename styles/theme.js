import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    fauxblack: "#454346",
  },
  fonts: {
    heading: "Fraunces",
    body: "Public Sans",
  },
});

export default theme;
