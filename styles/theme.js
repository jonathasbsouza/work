import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    fauxblack: "#454346",
    lightBeige: "#F8F4EF",
    white: "#fff",
  },
  fonts: {
    heading: "Fraunces",
    body: "Public Sans",
  },
});

export default theme;
